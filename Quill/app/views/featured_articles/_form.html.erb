<%= form_with(model: featured_article, local: true) do |form| %>
  <% if featured_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(featured_article.errors.count, "error") %> prohibited this featured_article from being saved:</h2>

      <ul>
        <% featured_article.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :article %>
    <%= form.text_field :article %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
